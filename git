a1.Version Control-CVS,SVN,GT
Central:SVN/CVSConsider to 2 servers: all the changes in 2 servers are send to the central system.
No communication betwwen 2 servers,single point of copy

2.Distributed system has entire copy of original file -fork

3.Differnce between git and github?
Git                                          GitHub
Open source-Download and implement          usuablity-(issues commenting reviewing project management)

ls .git has head,config,hocks,objects,refs
config -secrets
refs&objects-all files created


Branch strategy:To isolate
main/master branch
feature branch
release branch-test and deliver to the customer

#git commands
git init -initialize git, creates .git for tracking,logging
git add .-add all changes
git diff -shows the different commits
git commit -m
git status
git push
git log --oneline
git clone url-download file from remote using ssh
ssh-keygen -t rsa-create public key and add to github
git fork-used to create a copy of the repository
git branch-create new branch
git checkout -b branchname-creates and checkout
git remote -v -shows remote
git remote add -to add remote
git merge-not linear commits
git rebase-linear commits

git cherry-pick-easy to manage 1 or 2 commits
